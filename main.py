import config
# from model import train_classes
# from data_preprocess import clean_data
while True:
    user = str(input("Bot: Welcome to simple chat. How can I help you? [Enter 'q' for quit]\nMe: "))     
    if user.lower() == 'q':
        print("Bot: See you again!")
        break
    print("Bot: At this moment chatbot is in developement. Will be available soon!")
    break
    #process the input and do text preprocessing and get the vector and predict the intent
    
    # cleaned_text = clean_data(user)

    
